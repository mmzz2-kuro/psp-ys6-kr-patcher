# 프로젝트 작업 규칙

이 저장소에서 수행하는 모든 작업은 아래 규칙을 따른다.

## 1. 도구 배치

- 사용자가 직접 실행하는 한글 패치 도구는 GUI를 포함하여 `/tools`에 작성한다.
- GUI 없이 내부 처리나 자동화에 사용하는 스크립트는 `/tools/scripts`에 작성한다.
- Python 기반 GUI 도구는 `/tools`에 작성한다.
- Python 기반 비GUI 스크립트는 `/tools/scripts`에 작성한다.
- Node.js를 사용하는 스크립트는 `/tools/scripts/nodejs`에 작성한다.

## 2. 개발 언어

- 개발 언어는 가능한 한 하나로 통일한다.
- GUI는 가급적 Python으로 제작한다.
- 작업 특성상 필요하면 다른 언어를 사용할 수 있으나, 그 이유와 적용 범위를 계획 문서에 기록한다.

## 3. 필수 작업 절차

모든 구현, 분석, 패치 및 변경 작업은 다음 순서를 따른다.

1. 계획 수립
2. `/docs/plan`에 계획 문서 작성
3. 사용자에게 계획 확인 요청
4. 사용자 확인 후 작업 진행
5. `/docs/result`에 결과 문서 작성

- 사용자 확인 전에는 계획 범위를 실제로 구현하거나 원본 및 패치 ROM에 적용하지 않는다.
- 진행 중 계획이 바뀌면 계획 문서를 먼저 갱신하고, 중요한 범위 변경은 다시 사용자에게 확인받는다.
- 완료된 계획 문서는 상황에 따라 완료 상태로 갱신한 뒤 `/docs/result`로 이동할 수 있다.
- 결과 문서에는 수행 내용, 생성·변경 파일, 검증 결과, 알려진 문제와 정리 대상 임시 ROM을 기록한다.

## 4. ROM 취급

- 원본 ROM은 가급적 직접 수정하지 않는다.
- 원본 ROM을 입력으로 읽고 별도의 패치 작업본에 변경을 적용하는 방식을 우선한다.
- 수정 ROM을 처리할 때 매 작업마다 불필요하게 새 ROM을 만들지 않는다.
- 격리 테스트 등으로 여러 ROM이 필요한 경우 생성 이유와 용도를 기록한다.
- 이슈가 완료되면 해당 이슈에서 생성한 불필요한 테스트 ROM을 식별하여 삭제할 수 있도록 결과 문서에 정리한다.
- 파일 삭제는 정확한 대상과 복구 가능성을 확인한 뒤 수행한다.

## 5. 패치 ROM 배치

- 수정된 ROM은 `/patched` 아래에 둔다.
- 수정 내용이나 이슈별로 `/patched` 아래에 하위 폴더를 만들 수 있다.
- 같은 이슈에 속한 작업본은 같은 이슈 폴더에서 관리한다.
- 파일명이나 결과 문서만 보고도 원본 계열, 이슈, 용도 및 상태를 구분할 수 있게 관리한다.

## 6. 규칙 우선순위

- 이 문서는 이 저장소의 상시 작업 규칙이다.
- 개별 계획 문서는 이 규칙을 구체화할 수 있지만 임의로 완화하거나 생략할 수 없다.
- 예외가 필요하면 작업 전에 이유, 범위, 위험 및 복구 방법을 계획 문서에 명시하고 사용자 확인을 받는다.
